# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Optional: Telegram bot token for QR login
TELEGRAM_BOT_TOKEN=

# Database path (used inside Docker container)
DATABASE_PATH=/app/data/gestao_ti.db
